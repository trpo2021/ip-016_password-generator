#pragma once

#include <iostream>
#include <ctime>
#include <time.h>
#include <random>

using namespace std;


int selectArray();
int selectAction(int opt);
char* generate_password(int length);
int getKey();
