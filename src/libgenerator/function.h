#pragma once

#include <ctime>
#include <iostream>
#include <random>
#include <time.h>

using namespace std;

int selectArray();
int selectAction(int opt);
char* generate_password(int length);
int getKey();
